services:
  - type: web
    name: vape-shop
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: vape-shop-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
