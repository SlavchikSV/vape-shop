# Исключаем саму базу данных из коммитов
shop.db
*.db-journal
*.db-wal
*.db-shm

# Временные файлы бэкапов (они создаются, коммитятся, потом удаляются)
shop_backup_*.db

# Остальное как обычно
__pycache__/
*.py[cod]
.env
.venv/
*.log
